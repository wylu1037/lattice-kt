description = 'lattice'

dependencies {
    implementation project(path: ':model')
    implementation project(path: ':abi')
    implementation(
            "com.squareup.okhttp3:okhttp",
            "com.google.code.gson:gson",
    )
}
